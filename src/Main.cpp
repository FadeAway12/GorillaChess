#include <bitset>
#include <iostream>

#include "UCI.h"
#include "Board.h"

/*
int main() {
	Board::WP = blockermask_bishop(27);
	std::cout  << std::popcount(Board::WP);
	Board::printBoard();
	UCI::UCI();

}



*/

	/*
	* Used to hash the moves available for the rook by blocker boards.
	* magicRook & magicBishop store the magic numbers for each square.
	*/
/*
std::uint64_t** rookMoves[64];
int magicRook[64];

std::uint64_t** bishopMoves[64];
int magicBishop[64];

void initialize() {

}

int getMagic() {
	int max = pow(2, 32);
	int min = 0;
	srand(time(NULL)); // Seed the time
	int finalNum = rand() % (max - min + 1) + min; // Generate the number, assign to variable.
	return finalNum;
}

*/


